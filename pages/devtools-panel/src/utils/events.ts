export const pageConnectionEvent = () => 'page-connection';

export const pageConfigEvent = () => 'page-config';

export const controlUpdateEvent = (controlId: string) => `control-update-${controlId}`;
